{{- /*
  Custom RSS feed. Emits the FULL post body in <content:encoded> (the
  standard for full-content RSS, what dev.to and most aggregators read)
  while keeping <description> as the short summary for preview-style
  readers.

  Widget rendering for the RSS output is handled by the format-specific
  partners of these files:
    layouts/_default/_markup/render-codeblock-*.rss.xml
    layouts/shortcodes/*.rss.xml
  Each emits a plain <pre><code> block (for source-bearing widgets) or a
  one-line link back to the post (for parameterized shortcodes), so the
  generated drafts on dev.to don't contain broken iframes or notebook
  runtime scripts.
*/ -}}
{{- $pctx := . -}}
{{- if .IsHome -}}{{ $pctx = .Site }}{{- end -}}
{{- $pages := slice -}}
{{- if or $.IsHome $.IsSection -}}
{{- $pages = $pctx.RegularPages -}}
{{- else -}}
{{- $pages = $pctx.Pages -}}
{{- end -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
{{- $pages = $pages | first $limit -}}
{{- end -}}
{{- printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>{{ if eq  .Title  .Site.Title }}{{ .Site.Title }}{{ else }}{{ with .Title }}{{.}} on {{ end }}{{ .Site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne  .Title  .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}on {{ .Site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Author.email }}
    <managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Author.email }}
    <webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS" -}}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{- end -}}
    {{ range $pages }}
    {{- $safeContent := replace .Content "]]>" "]]]]><![CDATA[>" -}}
    {{- /* Language tags (EN/PT/PT-BR) double as i18n markers in the
           taxonomy, but they aren't meaningful topics for an RSS reader
           or dev.to import — drop them from <category> output entirely.
           Match is case-insensitive so "en", "En", etc. all skip. */ -}}
    {{- $langTags := slice "en" "pt" "pt-br" -}}
    {{- /* Build a lowercase set of the post's tag titles. Keywords that
           match a tag (case-insensitively) are skipped below so we don't
           emit the same term twice as <category> — common in posts like
           circ-en where "WebAssembly" and "DSL" appear in both lists. */ -}}
    {{- $tagTitlesLower := slice -}}
    {{- range .GetTerms "tags" -}}
      {{- $tagTitlesLower = $tagTitlesLower | append (lower .Title) -}}
    {{- end -}}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
      <guid>{{ .Permalink }}</guid>
      {{- /* Tags are a Hugo taxonomy, so each term has a canonical
             listing page. Emitting the URL via domain= lets RSS readers
             link the category. dev.to ignores the attribute and uses the
             text as a plain tag. */ -}}
      {{- range .GetTerms "tags" }}
        {{- if not (in $langTags (lower .Title)) }}
      <category domain="{{ .Permalink }}">{{ .Title }}</category>
        {{- end }}
      {{- end }}
      {{- /* Keywords are a plain frontmatter list (not a taxonomy), so
             no URL. uniq drops within-list duplicates (e.g. "Binary"
             twice in the-binary-en); the case-insensitive check above
             drops cross-list duplicates with tags. */ -}}
      {{- range .Params.keywords | uniq }}
        {{- $kwLower := lower . -}}
        {{- if and (not (in $tagTitlesLower $kwLower)) (not (in $langTags $kwLower)) }}
      <category>{{ . }}</category>
        {{- end }}
      {{- end }}
      <description>{{ .Summary | html }}</description>
      <content:encoded>{{ printf "<![CDATA[%s]]>" $safeContent | safeHTML }}</content:encoded>
    </item>
    {{ end }}
  </channel>
</rss>
