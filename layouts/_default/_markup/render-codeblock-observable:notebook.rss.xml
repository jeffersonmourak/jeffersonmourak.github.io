{{- /*
  RSS variant of the observable:notebook code-block render hook. The
  HTML variant inlines a built Observable Notebook bundle + runtime;
  for RSS we emit a one-line "view on the blog" paragraph matching the
  binaryCounter shortcode override.
*/ -}}
<p><em>Interactive Observable notebook widget &mdash; <a href="{{ .Page.Permalink }}">view it on the blog</a>.</em></p>
