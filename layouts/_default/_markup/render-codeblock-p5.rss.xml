{{- /*
  RSS variant of the p5 code-block render hook. The HTML variant mounts
  a live p5.js sketch via inline <script>; for RSS we emit a one-line
  "view on the blog" paragraph matching the binaryCounter shortcode
  override.
*/ -}}
<p><em>Interactive p5 widget &mdash; <a href="{{ .Page.Permalink }}">view it on the blog</a>.</em></p>
